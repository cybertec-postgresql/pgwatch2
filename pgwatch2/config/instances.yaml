- unique_name: test1  # functions also as prefix for found DBs if using patterns/continous discovery
  dbtype: postgres    # postgres|pgbouncer|postgres-continuous-discovery
  host: localhost
  port: 5432
  dbname:
  user: krl
  password: krl
  sslmode: disable
  stmt_timeout: 5
  is_superuser: true
  preset_metrics: null
  custom_metrics: {"wal": 10}
  dbname_include_pattern: bench
  dbname_exclude_pattern: be$
  is_enabled: true

- unique_name: test2
  dbtype: postgres
  host: localhost
  port: 5432
  dbname: ''
  user: krl
  password: krl
  sslmode: disable
  stmt_timeout: 5
  preset_metrics:
  custom_metrics: {a: 1}
  dbname_include_pattern: (bar|postgres)
  dbname_exclude_pattern:
  is_enabled: false
